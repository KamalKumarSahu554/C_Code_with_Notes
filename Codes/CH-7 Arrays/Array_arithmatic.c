// All arithematic operation performed (i++,++i,i--,--i)
#include<stdio.h>

int main()
{
     // // int store 4 bytes of memory (Acording to archtechture)
     // int i = 34;
     // int *ptr = &i;
     // printf("the vale of ptr is : %u\n", ptr);
     // //ptr++;
     // ptr--;
     // printf("the vale of ptr is : %u\n", ptr);

     // // float store 4 bytes of memory (Acording to archtechture)
     // float i = 34;
     // float *ptr = &i;
     // printf("the vale of ptr is : %u\n", ptr);
     // //ptr++;
     // ptr--;
     // printf("the vale of ptr is : %u\n", ptr);

     // char store 4 bytes of memory (Acording to archtechture)
     char i = 34;
     char *ptr = &i;
     printf("the vale of ptr is : %u\n", ptr);
     //ptr++;
     ptr--;
     printf("the vale of ptr is : %u\n", ptr);
     return 0;
}